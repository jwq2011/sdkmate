# Bash 本地配置扩展
# 此文件用于存放机器-specific 的配置，不会被部署到其他服务器
# 部署到 ~/.bashrc.local

# ==================== 本机特定配置 ====================
# 在这里添加只有本机需要的配置
# 例如：
# alias myserver='ssh user@specific-server'

# ==================== 本地 PATH ====================
# 本地二进制目录
# export PATH="$HOME/.local/bin:$PATH"

# ==================== 本地函数 ====================
# 只在本机使用的函数
# function myfunc() { ... }